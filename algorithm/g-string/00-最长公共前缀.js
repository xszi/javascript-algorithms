// 最长公共前缀
// 输入: ["flower","flow","flight"]
// 输出: "fl

const getLongestShareChildStr = (arr) => {
    if (!arr.length) return '';
    if (arr.length === 1) return arr[0];
    let childStr = []
    for (let i = 0; i < arr.length, i++) {
        arr[i] = arr[i]
    }
}